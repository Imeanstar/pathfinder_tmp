# Vertex AI Agent Engine 원격 런타임이 실제로 설치할 의존성 목록(agent_engines.create의
# requirements 인자로 그대로 넘어간다) — 로컬 개발 venv(.venv)와는 별개다.
#
# app/agents/supervisor.py의 그래프는 결정론적 코드만 쓴다(Gemini 등 LLM 호출이
# 그래프 안에 없음, app/llm.py·app/agents/chat.py만 LLM을 쓴다) — 그래서
# google-genai/langchain-google-genai는 전혀 필요 없다. google-cloud-aiplatform은
# 우리 코드가 아니라 Agent Engine 프레임워크 자체(텔레메트리 초기화, telemetry_utils.py의
# resolve_project_id)가 원격 컨테이너 안에서 요구한다 — 없으면 기동 자체가
# ModuleNotFoundError로 실패한다(2026-08-25 실제 배포에서 발견). 이 컨테이너는 Cloud
# Run 배포(google-genai 계열, protobuf<6.0)와 완전히 분리된 별도 환경이라, 여기서
# google-cloud-aiplatform이 요구하는 protobuf 6.x를 써도 Cloud Run 쪽엔 영향이 없다.

langgraph>=0.2
pdfplumber>=0.11
pyyaml>=6
google-cloud-aiplatform
