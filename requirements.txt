pytest>=9
pdfplumber>=0.11
pyyaml>=6
scikit-learn>=1.9
numpy>=1.26
langgraph>=0.2
mcp[cli]>=2.0
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
httpx>=0.27
reportlab>=4.2
python-dotenv>=1
# GOOGLE_API_KEY가 있을 때만 실제로 import됨(app/retrieval.py의 GeminiEncoder,
# app/llm.py의 _call_gemini) — 키 없이 개발 중인 지금은 설치 안 해도
# TF-IDF/빈결과 대체 경로로 전부 동작함. google-generativeai(구 SDK)는 폐지
# 예정이라 유지보수 중인 google-genai(신 SDK)를 쓴다.
google-genai>=1
google-auth>=2
# app/retrieval.py의 GeminiEncoder(하이브리드 RAG의 임베딩 절반)가 쓴다. 이게 빠져
# 있으면 GeminiEncoder() 생성이 ModuleNotFoundError를 던지고 make_encoder()의
# try/except가 조용히 삼켜서 TF-IDF 단독으로만 폴백한다 — 실제로 이 상태로
# 배포돼 있었다(2026-08-24 비용 최적화 작업 중 발견, RAG가 하이브리드로 동작한
# 적이 한 번도 없었음).
langchain-google-genai>=2

# 로그인 계정별 최신 로드맵 저장(app/user_store.py) — 로컬 개발엔 GCP 인증정보가 없어
# 항상 SQLite로 대체되지만, 실제 배포(Cloud Run)에서 쓸 코드 경로라 로컬에도 설치해
# import 자체가 깨지지 않는지 확인한다. **버전을 반드시 고정할 것** — 최신
# google-cloud-firestore(2.28+)는 protobuf>=6.33을 요구하는데, google-genai가 물고 오는
# google-ai-generativelanguage는 protobuf<6.0을 요구해서 서로 충돌한다(직접 겪음,
# 2026-08-21). grpcio-status도 최신판은 protobuf>=6.33을 요구해 같이 맞춰야 한다.
google-cloud-firestore==2.20.2
grpcio-status<1.71
